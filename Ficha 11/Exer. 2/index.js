// Crie uma função concatenate que recebe duas strings como argumentos e retorna uma nova string que contém as duas strings originais concatenadas

function concatenate() {
    const firstString = prompt("Introduz a primeira string")
    const secondString = prompt("Introduz a segunda string: ")
    return (firstString + secondString)
}

concatenate()
